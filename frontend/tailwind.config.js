module.exports = {
  darkMode: "class", // Enables class-based dark mode
  content: [
    "./src/**/*.{html,ts}", // Ensure all files are scanned for Tailwind classes
  ],
  theme: {
    extend: {}, // Extend default Tailwind theme if necessary
  },
  plugins: [],
};
